// In function expression function havn't name.
//It is one kind of anonymous function.

let add = function(a,b){
    return a + b;
}

console.log(add(10,20));

// If we give a name also in anonymous function then we not get any advantage